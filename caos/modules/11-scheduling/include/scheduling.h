#pragma once
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Среднее время ожидания при политике FCFS (First-Come-First-Served).
 * burst[i] — длительность процесса с id = i; все процессы пришли в момент 0
 * и выполняются строго по порядку индексов (0, 1, 2, ...).
 * Время ожидания процесса = сумма длительностей всех, кто стоит перед ним.
 * Возвращает среднее время ожидания по всем n процессам.
 * Для n <= 0 поведение не проверяется (тесты подают n >= 1). */
double fcfs_avg_waiting(const int *burst, int n);

/* Порядок исполнения процессов при Round-Robin (круговое планирование).
 * burst[i] — длительность процесса с id = i; все пришли в момент 0.
 * quantum — размер кванта времени (quantum >= 1).
 * Процессы обслуживаются по кругу в порядке индексов: каждому даётся не более
 * quantum единиц времени, затем, если у него остаётся работа, он встаёт в конец
 * очереди. order_out заполняется последовательностью id процессов в том порядке,
 * в каком им давали процессор (по одному id за каждый выданный квант).
 * В *order_len записывается итоговая длина этой последовательности.
 * Гарантируется, что order_out достаточно большой под все кванты. */
void round_robin_order(const int *burst, int n, int quantum,
                       int *order_out, int *order_len);

#ifdef __cplusplus
}
#endif
