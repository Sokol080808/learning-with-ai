#pragma once
#include <stddef.h>   // size_t
#include <stdint.h>   // привычно держать рядом, пригодится в твоих экспериментах

#ifdef __cplusplus
extern "C" {
#endif

/* Сумма первых n элементов массива a (a[0] + a[1] + ... + a[n-1]).
   Для n == 0 сумма равна 0. */
int sum_first_n(const int* a, int n);

/* Максимальный элемент среди первых n элементов массива a.
   Гарантируется, что n >= 1 (массив непустой). */
int max_in(const int* a, int n);

/* Развернуть первые n элементов массива a НА МЕСТЕ:
   было {1,2,3,4} -> станет {4,3,2,1}. Ничего не возвращает — меняет сам массив. */
void reverse_array(int* a, int n);

/* Сколько раз символ c встречается в си-строке s (до завершающего '\0'). */
size_t count_char(const char* s, char c);

/* -----------------------------------------------------------------------
 * Задание 5: хеш-таблица счётчиков частот слов.
 *
 * FreqTable — непрозрачная хеш-таблица word -> count.
 * Три бага намеренно посажены внутри — найди и почини.
 * ----------------------------------------------------------------------- */

/* Непрозрачный дескриптор таблицы.  */
typedef struct FreqTable FreqTable;

/* Создать пустую таблицу.  Возвращает NULL при ошибке аллокации. */
FreqTable* freq_create(void);

/* Освободить всю память таблицы.  freq_destroy(NULL) — безопасно. */
void freq_destroy(FreqTable* t);

/* Добавить слово word в таблицу (увеличить счётчик на 1).
   Возвращает 0 при успехе, -1 при ошибке аллокации. */
int freq_add(FreqTable* t, const char* word);

/* Вернуть число вхождений слова word.  Если слово не встречалось — 0. */
size_t freq_lookup(const FreqTable* t, const char* word);

/* Количество уникальных слов в таблице. */
size_t freq_unique(const FreqTable* t);

#ifdef __cplusplus
}
#endif
