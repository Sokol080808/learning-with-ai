#pragma once
#include <stddef.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Номер страницы для виртуального адреса addr.
 * page_size — размер страницы в байтах, ГАРАНТИРОВАННО степень двойки
 * (например 256, 1024, 4096). Адреса внутри одной страницы делят общий
 * номер: addr и addr+1 лежат на одной странице, пока не пересекут границу.
 * Номер страницы = addr / page_size (целочисленно). */
size_t page_number(size_t addr, size_t page_size);

/* Смещение (offset) адреса addr внутри его страницы.
 * page_size — размер страницы в байтах, степень двойки.
 * Смещение = addr % page_size; оно всегда в диапазоне [0, page_size - 1].
 * Связь: addr == page_number(addr) * page_size + page_offset(addr). */
size_t page_offset(size_t addr, size_t page_size);

/* Число страничных промахов (page faults) при политике замещения FIFO.
 * refs[0..n-1] — строка обращений: номера страниц в порядке доступа.
 * frames — сколько физических кадров (slot'ов) доступно (frames >= 1).
 * Память изначально пуста, поэтому первые обращения к ещё не загруженным
 * страницам — это «холодные» промахи, и они ТОЖЕ считаются.
 * Промах: обращение к странице, которой сейчас нет в памяти. Если все кадры
 * заняты, выселяется страница, загруженная РАНЬШЕ всех (First-In-First-Out),
 * независимо от того, как недавно к ней обращались.
 * Возвращает общее число промахов. Для n <= 0 тесты не подаются. */
int fifo_page_faults(const int *refs, int n, int frames);

/* Число страничных промахов при политике замещения LRU.
 * Параметры — как у fifo_page_faults. Отличие только в выборе жертвы:
 * при заполненной памяти выселяется страница, к которой дольше всего НЕ
 * обращались (Least Recently Used). Каждое обращение (в т.ч. попадание/hit)
 * обновляет «свежесть» страницы.
 * Возвращает общее число промахов (с учётом холодных). */
int lru_page_faults(const int *refs, int n, int frames);

/* ------------------------------------------------------------------ *
 *  Двухуровневая таблица страниц (симулятор трансляции адресов)       *
 * ------------------------------------------------------------------ *
 * Модель x86-подобной 32-битной трансляции. Виртуальный адрес режется
 * на три поля:
 *
 *   31      22 21      12 11         0
 *   +---------+----------+-----------+
 *   |  L1(10) |  L2(10)  | offset(12)|
 *   +---------+----------+-----------+
 *
 *   l1     = (vaddr >> 22) & 0x3FF   — индекс в директории (L1), 1024 записи
 *   l2     = (vaddr >> 12) & 0x3FF   — индекс во вложенной таблице (L2)
 *   offset = vaddr & 0xFFF           — смещение внутри страницы (4096 байт)
 *
 * L1-запись указывает на L2-таблицу (или пуста). L2-запись хранит номер
 * физического кадра. Физический адрес = (frame << 12) | offset.
 * Память под L2-таблицы выделяется ЛЕНИВО — только под реально
 * использованные участки адресного пространства (в этом весь смысл
 * многоуровневой схемы; см. README, «Идея 5»). */

#define PT_PAGE_BITS    12u                  /* бит на смещение -> page_size = 4096 */
#define PT_PAGE_SIZE    (1u << PT_PAGE_BITS)  /* 4096 байт */
#define PT_INDEX_BITS   10u                   /* бит на каждый из двух индексов */
#define PT_ENTRIES      (1u << PT_INDEX_BITS) /* 1024 записи в L1 и в каждой L2 */

typedef struct PageTable PageTable;

/* Создаёт пустую таблицу страниц (ни одна страница не отображена).
 * Возвращает указатель, который надо потом освободить через pt_destroy,
 * либо NULL при нехватке памяти. */
PageTable *pt_create(void);

/* Освобождает таблицу и все ленивые L2-таблицы. pt_destroy(NULL) — no-op. */
void pt_destroy(PageTable *pt);

/* Транслирует виртуальный адрес vaddr в физический, проходя оба уровня.
 *   - Разбивает vaddr на (l1, l2, offset).
 *   - Спускается L1 -> L2. Если по пути встречает отсутствующую запись:
 *       * alloc == 0: трансляция невозможна, возвращает -1 (страница не
 *         отображена — это аналог page fault);
 *       * alloc != 0: создаёт недостающую L2-таблицу и/или назначает
 *         странице свежий физический кадр (по порядку, начиная с 0),
 *         как если бы ОС обработала промах и подложила кадр.
 *   - При успехе возвращает физический адрес = (frame << 12) | offset.
 * Возвращает int64_t, чтобы -1 нельзя было спутать с валидным адресом. */
int64_t pt_walk(PageTable *pt, uint32_t vaddr, int alloc);

/* Явно отображает страницу адреса vaddr на физический кадр адреса paddr.
 * Берётся только номер кадра обоих адресов: смещение paddr игнорируется
 * (отображаются СТРАНИЦЫ, а не байты). После pt_map(pt, v, p) вызов
 * pt_walk(pt, v, 0) обязан вернуть (frame_of(p) << 12) | (v & 0xFFF).
 * Недостающая L2-таблица создаётся при необходимости.
 * Возвращает 0 при успехе, -1 при нехватке памяти. */
int pt_map(PageTable *pt, uint32_t vaddr, uint32_t paddr);

#ifdef __cplusplus
}
#endif
