// ВНИМАНИЕ: здесь пишешь ТЫ. Реализуй vm_run так, чтобы приёмочные тесты
// капстоуна (tests/test_vm.cpp) стали зелёными.
//
// Это финальный проект курса: маленький «процессор в программе». Тебе дан только
// КОНТРАКТ (include/vm.h) и тесты — архитектуру внутри ты придумываешь сам. Это
// и есть смысл капстоуна: не дописать функцию по образцу, а спроектировать
// машину целиком.
//
// Сейчас это заглушка: vm_run всегда возвращает код ошибки и ничего не считает,
// поэтому ВСЕ тесты на успех падают (а тесты на ошибки могут случайно проходить —
// не обманись этим, настоящая реализация должна возвращать ПРАВИЛЬНЫЙ код для
// каждого случая). Когда напишешь цикл выборка-декодирование-исполнение —
// тесты позеленеют.
//
// План, если не знаешь, с чего начать (подробнее — в README.md):
//   1. Заведи массив-стек на VM_STACK_CAPACITY элементов и индекс вершины sp.
//   2. Заведи счётчик команд pc = 0. Крути цикл, пока pc в пределах программы.
//   3. На каждом витке: ВЫБОРКА — прочитай program[pc] как опкод; ДЕКОДИРОВАНИЕ —
//      switch по опкоду; ИСПОЛНЕНИЕ — сделай нужное со стеком и сдвинь pc.
//   4. На каждом снятии проверяй underflow, на каждом PUSH/DUP — overflow,
//      в DIV — деление на ноль. Любую беду возвращай нужным кодом из VmStatus.
//   5. HALT — выйти из цикла и записать вершину стека в *out_result (вернуть VM_OK).

#include "vm.h"

// Контракт см. в vm.h: выполнить программу, вернуть VM_OK и записать результат в
// *out_result при успехе, либо код ошибки из VmStatus.
int vm_run(const int* program, size_t len, int* out_result) {
    (void)program;     // убери, когда напишешь реализацию
    (void)len;         // убери, когда напишешь реализацию
    (void)out_result;  // убери, когда напишешь реализацию

    // TODO: реализовать цикл выборка-декодирование-исполнение со стеком.
    // Пока возвращаем «ошибку», чтобы тесты на успех были красными.
    return VM_ERR_BAD_OPCODE;
}
