[pytest]
# Тесты курса. Импорт по умолчанию (prepend): каталог с тест-файлом попадает в sys.path,
# поэтому `from <slug> import ...` находит стаб-файл рядом. Поэтому имена стаб- и тест-файлов
# во всех модулях уникальны.
testpaths = modules capstone
python_files = test_*.py
addopts = -q
