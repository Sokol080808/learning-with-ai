# ВНИМАНИЕ: здесь пишешь ТЫ. Реализуй функции так, чтобы тесты модуля 01 стали зелёными.
#
# Каждая функция сейчас кидает NotImplementedError — это «дырка», которую ты заполняешь.
# Сигнатуры и контракт менять не нужно: тесты рассчитывают именно на них.


def to_fahrenheit(c: float) -> float:
    """Перевести температуру из градусов Цельсия в градусы Фаренгейта.

    Формула: C * 9 / 5 + 32.
    Результат — float (например, 0 -> 32.0, 100 -> 212.0).
    """
    raise NotImplementedError("TODO: верни температуру по формуле c * 9 / 5 + 32")


def is_even(n: int) -> bool:
    """Вернуть True, если n чётное, иначе False.

    Подсказка: чётное число делится на 2 без остатка.
    """
    raise NotImplementedError("TODO: проверь чётность через остаток от деления на 2")


def min_of_three(a: int, b: int, c: int) -> int:
    """Вернуть наименьшее из трёх чисел a, b, c."""
    raise NotImplementedError("TODO: верни наименьшее из трёх чисел")


def average3(a: int, b: int, c: int) -> float:
    """Вернуть среднее арифметическое трёх чисел.

    Результат должен быть ТОЧНЫМ дробным (float): следи за тем, каким делением
    пользуешься.
    """
    raise NotImplementedError("TODO: верни (a + b + c), делённое на 3, ТОЧНЫМ делением")


def greet(name: str) -> str:
    """Вернуть приветствие вида "Привет, {name}!".

    Например, greet("Аня") -> "Привет, Аня!".
    """
    raise NotImplementedError("TODO: собери строку приветствия через f-строку")


def parse_number(s: str) -> "int | float | None":
    """Безопасно распарсить строку в число без исключений.

    Возвращает:
    - int,   если строка представляет целое число (например, "42", "-7", " 10 ")
    - float, если строка представляет дробное число ("3.14", "-0.5", "1e3")
    - None,  если строка — мусор, пустая или не является числом
    """
    raise NotImplementedError("TODO: попробуй сначала int(s.strip()), затем float(s.strip()), при ошибке — вернуть None")
