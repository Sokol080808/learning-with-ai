# ВНИМАНИЕ: здесь пишешь ТЫ. Реализуй функции так,
# чтобы тесты модуля 10 стали зелёными.
#
# Это один из файлов пакета `mathx`. Здесь живёт "арифметический" подмодуль.
# Снаружи (из тестов) функции видны как `from mathx import add, mul` —
# за это отвечает реэкспорт в mathx/__init__.py.


def add(a: int, b: int) -> int:
    """Вернуть сумму двух целых: a + b.

    Контракт:
      add(2, 3) == 5
      add(-1, 1) == 0
      add(0, 0) == 0
    """
    raise NotImplementedError("TODO: верни a + b")


def mul(a: int, b: int) -> int:
    """Вернуть произведение двух целых: a * b.

    Контракт:
      mul(2, 3) == 6
      mul(-4, 5) == -20
      mul(7, 0) == 0
    """
    raise NotImplementedError("TODO: верни a * b")
