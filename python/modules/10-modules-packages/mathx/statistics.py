# ВНИМАНИЕ: здесь пишешь ТЫ. Реализуй четыре функции,
# чтобы тесты существенного задания стали зелёными.
#
# Это третий подмодуль пакета `mathx`. Он занимается описательной статистикой:
# принимает список чисел и возвращает характеристику выборки.
# Снаружи имена видны как `from mathx import mean, median, mode, variance`.
#
# ЗАПРЕЩЕНО импортировать стандартный модуль `statistics` из stdlib:
# ты должен реализовать алгоритмы самостоятельно. `math`, `collections` — можно.

from __future__ import annotations

from typing import Sequence


def mean(data: Sequence[float]) -> float:
    """Среднее арифметическое выборки.

    Контракт:
      mean([1, 2, 3]) == 2.0
      mean([0.0])     == 0.0
      mean([-1, 1])   == 0.0

    Краевые случаи:
      - Пустая последовательность → поднять ValueError.
      - Принимает любой Sequence[float]: список, кортеж.
    """
    raise NotImplementedError("TODO: реализуй среднее арифметическое")


def median(data: Sequence[float]) -> float:
    """Медиана выборки (значение, делящее отсортированный ряд пополам).

    Для нечётного количества элементов — средний элемент отсортированного ряда.
    Для чётного — среднее двух центральных элементов.

    Контракт:
      median([3, 1, 2])     == 2.0
      median([4, 1, 3, 2])  == 2.5
      median([7])           == 7.0

    Краевые случаи:
      - Пустая последовательность → поднять ValueError.
      - Исходный порядок данных НЕ должен меняться (работай с копией).
    """
    raise NotImplementedError("TODO: реализуй медиану")


def mode(data: Sequence[float]) -> float:
    """Мода: наиболее часто встречающийся элемент выборки.

    Если несколько значений встречаются одинаково часто — вернуть
    наименьшее из них (для детерминизма и простоты).

    Контракт:
      mode([1, 2, 2, 3])         == 2
      mode([1, 1, 2, 2, 3])      == 1   # ничья → наименьший
      mode([5])                  == 5
      mode([3, 3, 1, 1, 2, 2])   == 1   # тройная ничья → наименьший

    Краевые случаи:
      - Пустая последовательность → поднять ValueError.
    """
    raise NotImplementedError("TODO: реализуй моду")


def variance(data: Sequence[float], ddof: int = 0) -> float:
    """Дисперсия выборки.

    ddof (degrees of freedom correction):
      ddof=0 → генеральная дисперсия  (делить на N)
      ddof=1 → выборочная дисперсия   (делить на N-1, исправленная, Бессель)

    Формула: sum((x - mean(data)) ** 2 for x in data) / (N - ddof)

    Контракт:
      variance([2, 4, 4, 4, 5, 5, 7, 9])       == 4.0       # ddof=0
      variance([2, 4, 4, 4, 5, 5, 7, 9], ddof=1) == 4.571...  # ddof=1
      variance([42.0])                           == 0.0       # одна точка, ddof=0

    Краевые случаи:
      - Пустая последовательность → поднять ValueError.
      - len(data) < 2 при ddof=1 → поднять ValueError
        (нельзя делить на N-1 = 0).
      - ddof не равен 0 или 1 → поднять ValueError.
    """
    raise NotImplementedError("TODO: реализуй дисперсию")
