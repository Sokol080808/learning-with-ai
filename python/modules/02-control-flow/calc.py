# ВНИМАНИЕ: здесь пишешь ТЫ. Реализуй функцию так, чтобы тесты модуля 02 стали зелёными.
#
# Это ядро микро-проекта «CLI-калькулятор». Теория и подсказки — в README.md модуля.
# Готового решения здесь нет: функция пока кидает NotImplementedError.


def calc(a: float, op: str, b: float) -> float:
    """Применить арифметическую операцию op к числам a и b.

    Контракт:
      - op == "+"  -> a + b
      - op == "-"  -> a - b
      - op == "*"  -> a * b
      - op == "/"  -> a / b
    Ошибки (оба случая — raise ValueError с понятным сообщением):
      - деление на ноль (op == "/" и b == 0);
      - неизвестная операция (op не из "+", "-", "*", "/").
    Пример: calc(2, "+", 3) == 5.0; calc(10, "/", 4) == 2.5.
    """
    raise NotImplementedError("TODO: реализуй calc по контракту из docstring")
