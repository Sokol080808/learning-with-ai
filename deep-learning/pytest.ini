[pytest]
# Тесты курса Deep Learning. Импорт по умолчанию (prepend): каталог с тест-файлом попадает в
# sys.path, поэтому `from <slug> import ...` находит стаб рядом. Имена стаб- и тест-файлов уникальны.
testpaths = modules capstone
python_files = test_*.py
addopts = -q
