# Крейт-член воркспейса. Имя пакета совпадает с тем, что подключают тесты:
#   use m02_control_flow::*;
# Тебе НЕ нужно редактировать этот файл во время прохождения модуля.
[package]
name = "m02_control_flow"
version = "0.1.0"
edition = "2021"

[dependencies]
