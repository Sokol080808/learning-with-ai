# Модуль 15 — Конкурентность. Крейт-член воркспейса курса.
# Зависимостей нет — только стандартная библиотека (std::thread, std::sync).
# Редактировать этот файл во время прохождения не нужно.
[package]
name = "m15_concurrency"
version = "0.1.0"
edition = "2021"

[dependencies]
