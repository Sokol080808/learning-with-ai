#include "warmup.hpp"

// ВНИМАНИЕ: здесь намеренно оставлены БАГИ. Программа компилируется, но тесты падают.
// Твоя задача — починить тела функций так, чтобы тесты модуля 00 стали зелёными.

int add(int a, int b) {
    return a - b;  // TODO: должна быть сумма, а не разность
}

long seconds_in(int hours) {
    return hours * 60;  // TODO: это секунды в минутах, а не в часах
}

// Задание 2. ОПРЕДЕЛЕНИЕ функции, объявленной в warmup.hpp.
// Сигнатура обязана совпадать с объявлением до буквы, иначе линковщик
// не свяжет вызов с этим телом.
long abs_diff(int a, int b) {
    // TODO: верни модуль разности |a - b| (неотрицательное число).
    return a - b;  // сейчас может быть отрицательным — это баг, который ты чинишь
}
