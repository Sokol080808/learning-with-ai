#pragma once
// warmup.hpp — интерфейс разминочного задания.
// #pragma once гарантирует, что этот заголовок подключится не больше одного раза
// в один translation unit (защита от двойного включения).

// Возвращает сумму a и b.
int add(int a, int b);

// Возвращает количество секунд в указанном числе часов.
long seconds_in(int hours);
