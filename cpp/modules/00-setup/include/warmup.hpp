#pragma once
// warmup.hpp — интерфейс разминочного задания.
// #pragma once гарантирует, что этот заголовок подключится не больше одного раза
// в один translation unit (защита от двойного включения).

// Возвращает сумму a и b.
int add(int a, int b);

// Возвращает количество секунд в указанном числе часов.
long seconds_in(int hours);

// ---------------------------------------------------------------------------
// Задание 2. Объявление здесь, определение — в src/warmup.cpp.
//
// Здесь только ОБЪЯВЛЕНИЕ (прототип): сигнатура и точка с запятой, без тела.
// Так заголовок обещает: «такая функция где-то есть». Реальное тело ты напишешь
// в src/warmup.cpp — это две РАЗНЫЕ стадии (компиляция знает про объявление,
// линковка находит определение). Если определения не будет — будет ошибка
// ЛИНКОВКИ "undefined reference", а не компиляции. Прочувствуй эту разницу.
//
// abs_diff(a, b) возвращает модуль разности |a - b| (всегда неотрицательное число).
long abs_diff(int a, int b);

// ---------------------------------------------------------------------------
// Задание 3. inline-функция целиком в заголовке.
//
// Эта функция определена ПРЯМО ЗДЕСЬ, вместе с телом. Ключевое слово inline
// разрешает иметь одно и то же определение в нескольких translation units
// (каждый .cpp, включивший этот заголовок, получит свою копию) — без него
// линковщик ругался бы на нарушение ODR ("multiple definition").
//
// is_even(x) возвращает true, если x чётное, и false иначе.
inline bool is_even(int x) {
    // TODO: верни true для чётных x. Сейчас тут заведомо неверная заглушка.
    return false;
}
