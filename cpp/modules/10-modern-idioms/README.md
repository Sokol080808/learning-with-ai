# Модуль 10 — Современные идиомы C++

Этот модuль — про то, как пишут на C++ *сегодня*. Те же задачи, но короче, безопаснее и
выразительнее. Большая идея: современные возможности убирают шум и переносят больше
проверок на этап компиляции.

## Идея 1. `auto` — вывод типа и его подводные камни

`auto` просит компилятор вывести тип переменной из инициализатора. Внешне это «меньше
печатать», но механика глубже: правила вывода `auto` — это **те же правила, что у вывода
шаблонных аргументов** (template argument deduction). Поэтому, чтобы понимать `auto`, надо
понимать, что именно он отбрасывает.

### Как это работает под капотом

Когда вы пишете `auto x = expr;`, компилятор мысленно представляет шаблон
`template<class T> void f(T x);`, подставляет туда `expr` и смотрит, чему выведется `T`.
Ключевой момент: при выводе по значению (`auto x`) **отбрасываются** верхнеуровневые
`const`/`volatile` и ссылочность.
```cpp
const int  ci = 42;
auto a = ci;          // a -> int   (const отброшен!), a — независимая копия
a = 100;              // ок, ci не тронут

int& r = ...;
auto b = r;           // b -> int   (ссылочность отброшена), b — копия
```
Чтобы сохранить квалификаторы и не копировать, добавляйте их явно:
```cpp
const auto& c = ci;   // c -> const int&  (ссылка, без копии)
auto& d = r;          // d -> int&         (та же переменная)
```

### Зачем это нужно

1. **Громоздкие типы.** Тип итератора `std::map<std::string, std::vector<int>>::iterator`
   читать невозможно — `auto it = m.begin();` решает это.
2. **Типы, которых вы не можете назвать.** Тип лямбды уникален и не имеет имени — без
   `auto` его в переменную не положить:
   ```cpp
   auto cmp = [](int a, int b) { return a > b; };   // имя типа недоступно
   ```
3. **Защита от молчаливых сужающих конверсий.** `auto n = v.size();` всегда возьмёт точный
   тип `size_t`; а `int n = v.size();` молча сузит на больших векторах.

### Trade-offs и ловушки

- **Потеря читаемости.** `auto total = compute();` прячет тип. Если из имени функции тип
  не ясен — пишите тип явно. `auto` экономит автору, но может стоить читателю.
- **Случайная копия в range-for.** Самая частая дорогая ошибка:
  ```cpp
  for (auto s : strings) { ... }        // КОПИРУЕТ каждую строку!
  for (const auto& s : strings) { ... } // правильно: читаем без копий
  ```
- **`auto` никогда не выводит `&` сам.** `auto x = obj.big_member();` сделает копию, даже
  если функция вернула ссылку. Хотите ссылку — пишите `auto&`.
- **Proxy-объекты.** `std::vector<bool>` хранит биты, а `operator[]` возвращает прокси, а
  не `bool&`. `auto bit = vb[0];` поймает прокси-тип, а не `bool` — частый источник
  сюрпризов. Здесь лучше явный `bool bit = vb[0];`.

Правило большого пальца: **`auto&` / `const auto&` по умолчанию в циклах и при привязке к
существующим объектам; `auto` по значению — когда копия действительно нужна.**

## Идея 2. Structured bindings — разложить агрегат на имена

Structured bindings (C++17) позволяют за один шаг распаковать `pair`, `tuple`, массив или
структуру в несколько именованных переменных.
```cpp
std::pair<int, int> p = minmax_of(v);
auto [lo, hi] = p;                       // вместо p.first / p.second
```

### Как это работает под капотом

Запись `auto [a, b] = expr;` — это **синтаксический сахар**. Компилятор создаёт скрытую
переменную (назовём её `e`), инициализирует её из `expr`, а `a` и `b` делает **именами для
её частей**, а не самостоятельными переменными. Что считать «частью», зависит от типа:
- **массив** — поэлементно;
- **тип с `tuple_size`/`tuple_element`/`get` (`pair`, `tuple`, `array`)** — через `get<i>`;
- **простая структура без приватных полей** — публичные нестатические поля по порядку.

Из этого следуют важные практические выводы:
```cpp
auto [a, b] = p;        // e — КОПИЯ p; a,b ссылаются на части копии
auto& [a, b] = p;       // e — ссылка на p; запись в a меняет p.first
const auto& [a, b] = p; // только чтение, без копии — идеально для range-for
```
Квалификатор (`auto`, `auto&`, `const auto&`) применяется к **скрытой переменной `e`**, а
не к каждому имени по отдельности. Поэтому нельзя сделать одно имя ссылкой, а другое — нет.

### Зачем это нужно

Главный выигрыш — **читаемость в циклах по словарю** и при возврате нескольких значений:
```cpp
for (const auto& [name, count] : m)      // сразу ключ и значение, без it->second
    std::cout << name << " = " << count << "\n";

auto [quotient, remainder] = divmod(17, 5);   // говорящие имена вместо .first/.second
```

### Ловушки

- **Случайная копия.** `for (auto [k, v] : m)` копирует каждую пару (а ключ в `map` ещё и
  `const`, так что копия пары — это копия и ключа, и значения). В горячем цикле берите
  `const auto& [k, v]`.
- **Нельзя пропустить элемент.** В C++20 нет `_` как «игнорировать» — придётся завести имя
  (`auto [a, unused] = p;`) и, возможно, пометить его `(void)unused;`.
- **Имена — не настоящие переменные.** Их (в общем случае) нельзя захватить в лямбду по
  имени напрямую — это часто удивляет. Если нужно в лямбду, скопируйте в обычную переменную.
- Привязка к временному объекту по `const auto&` продлевает его жизнь (как обычная
  ссылка), а по `auto&` к rvalue — ошибка компиляции. Это защищает вас.

## Идея 3. `range-for` — как он устроен на самом деле

Цикл `for (auto x : range)` выглядит примитивно, но под ним прячется развёртка, понимание
которой убирает целый класс багов. Компилятор переписывает
```cpp
for (T x : range) body;
```
примерно в:
```cpp
{
    auto&& __r = range;                 // ссылка на диапазон, продлевает жизнь временного
    auto __b = std::begin(__r);
    auto __e = std::end(__r);
    for (; __b != __e; ++__b) {
        T x = *__b;                     // <-- здесь происходит копия или ссылка
        body;
    }
}
```
Отсюда два важных следствия.

**Первое — стоимость `x`.** Строка `T x = *__b;` — это то место, где `auto x` делает копию,
а `const auto& x` — нет. Для тяжёлых элементов (строки, вектора) это разница между O(n)
лишних аллокаций и нулём.

**Второе — опасность временных диапазонов.** `__r` связывается с `range`. Если `range` —
временный объект, он живёт до конца цикла (хорошо). Но если вы итерируетесь по *под-объекту*
временного, который сам не продлевается, получаете висячую ссылку:
```cpp
for (char c : make_string().substr(1)) { ... }   // ок: substr возвращает temporary, он жив
for (auto& kv : get_map_ref_member_of_temp()) {} // ОПАСНО, если temporary умирает раньше
```
В C++20 для этого добавили инициализатор в range-for, чтобы «удержать» владельца:
```cpp
for (auto&& obj = make_owner(); auto& part : obj.parts()) { ... }   // obj живёт весь цикл
```

Резюме: **`const auto&` — почти всегда правильный выбор элемента**; `auto&`, если вы
изменяете элементы на месте; `auto` по значению — только когда нужна именно копия.

## Идея 4. `enum class` — типобезопасные перечисления

```cpp
enum class Color { Red, Green, Blue };

Color c = Color::Red;          // обязательно с префиксом Color::
// int x = c;                  // ошибка — не приводится неявно к int (это хорошо!)
int x = static_cast<int>(c);   // если очень надо — явно
```
В отличие от старого `enum`, `enum class` не «протекает» именами в окружающую область и
не превращается случайно в int. Всегда предпочитай `enum class`. Можно задать и
базовый тип (`enum class Code : std::uint8_t { ... }`), чтобы зафиксировать размер.

## Идея 5. `constexpr` — перенос вычислений на этап компиляции

`constexpr` помечает функцию или переменную как **пригодную для вычисления компилятором**.
Это не «всегда вычисляется в компайл-тайме» — это «**может** быть вычислено в компайл-тайме,
если этого потребует контекст и если все аргументы — константные выражения».
```cpp
constexpr int square(int n) { return n * n; }

constexpr int k = square(5);          // ОБЯЗАН быть посчитан компилятором -> 25
static_assert(square(4) == 16);       // проверка корректности на этапе компиляции
int runtime = square(x);              // x неизвестен -> та же функция работает в рантайме
```

### Как это работает под капотом

Компилятор содержит мини-интерпретатор C++ («constant evaluator»). В **константном
контексте** (`constexpr`-переменная, `static_assert`, размер массива, нетиповой шаблонный
аргумент) он обязан выполнить функцию во время компиляции — иначе ошибка. В обычном
контексте `constexpr`-функция компилируется в обычный код и работает в рантайме. То есть
один и тот же исходник обслуживает оба мира.

### Зачем это нужно

- **Производительность даром.** Таблицы, хэши, разворачивание — посчитаны один раз
  компилятором, в бинарнике лежит уже результат.
- **Сильные гарантии.** Если выражение вычислилось в компайл-тайме, в нём **гарантированно
  нет UB** — компилятор откажется компилировать константное выражение с UB. Это бесплатный
  статический анализатор.
- **`static_assert` как тест.** Проверки уезжают из рантайма в сборку.

### Trade-offs и ловушки

- **`constexpr`-функции живут в заголовках.** Чтобы вычислить функцию в компайл-тайме в
  *другом* файле, компилятор должен видеть её **тело** — как и шаблоны (модуль 07).
  Объявление в `.hpp`, определение в `.cpp` лишит её способности считаться в компайл-тайме.
- **`constexpr` подразумевает `inline`** для функций — несколько определений в разных TU не
  нарушат ODR. Это ещё одна причина держать их в заголовке.
- **Не всё разрешено.** Внутри `constexpr` нельзя бросать неперехваченные исключения в
  константном контексте, нельзя использовать `reinterpret_cast`, нельзя вызывать обычные
  не-`constexpr` функции в константном вычислении. (В C++20 многое ослаблено: появились
  `constexpr`-аллокации, `std::vector`/`std::string` в `constexpr`, виртуальные вызовы.)
- **Не путайте с `const`.** `const` — «не изменю после инициализации» (рантайм-обещание).
  `constexpr` — «вычислимо компилятором» (компайл-тайм-категория). `constexpr` сильнее.
- Хотите *заставить* компайл-тайм даже там, где контекст не требует, — в C++20 есть
  `consteval` (функция обязана всегда вычисляться в компайл-тайме). В этом модуле мы её не
  трогаем, но знать полезно.

## Идея 6. Ranges и views (C++20) — конвейеры над данными

```cpp
#include <ranges>
namespace rv = std::views;

auto result = v
    | rv::filter([](int x){ return x % 2 == 0; })   // оставить чётные
    | rv::transform([](int x){ return x * x; });     // возвести в квадрат
std::vector<int> out;
for (int x : result) out.push_back(x);              // материализуем в вектор
```

### Как это работает под капотом

`rv::filter(pred)` и `rv::transform(f)` возвращают **view** — лёгкий объект, который
*не хранит* данные, а только ссылается на исходный диапазон и помнит операцию. Оператор `|`
просто «надевает» один view на другой (это range adaptor). Никакой работы при построении
конвейера не происходит — вся логика прячется в итераторе результата: когда вы делаете
`++it` и `*it`, он на лету прогоняет элемент через всю цепочку. Отсюда слово **ленивые**.

### Зачем это нужно и trade-offs

- **Нет промежуточных контейнеров.** Классический `std::transform` в новый вектор, потом
  `std::copy_if` в ещё один — это две аллокации и два прохода. Конвейер views — ноль
  промежуточных аллокаций, один проход при материализации.
- **Читается как описание намерения**, а не как механика циклов.
- **Ловушка времён жизни (висячие views).** View *ссылается* на источник. Если источник —
  временный объект, view может стать висячим:
  ```cpp
  auto bad = make_vector() | rv::filter(...);   // make_vector() умер — view висит!
  ```
  Материализуйте сразу или держите источник живым.
- **Однопроходность некоторых views.** Не все views можно обойти дважды; `filter`-view
  кэширует первый подходящий элемент. Для возврата из функции почти всегда нужно
  **материализовать** результат в `std::vector`, а не возвращать сам view.

## Идея 7. `[[nodiscard]]` — результат нельзя выбросить молча

Атрибут `[[nodiscard]]` (C++17) ставится на функцию (или на тип) и заставляет компилятор
**выдать предупреждение, если возвращённое значение проигнорировано**.
```cpp
[[nodiscard]] std::pair<int,int> divmod(int a, int b);

divmod(17, 5);            // warning: ignoring return value of 'divmod' [-Wunused-result]
auto [q, r] = divmod(17, 5);   // ок: результат использован
```

### Зачем это нужно

Возврат значения часто **и есть весь смысл** вызова. Если его проигнорировали — почти
наверняка баг:
- функции-фабрики и чистые вычисления (`divmod`, `parse`, `sqrt`) — игнор результата
  означает выброшенную работу;
- **коды ошибок.** `[[nodiscard]] Status do_thing();` — нельзя «забыть» проверить статус;
- **RAII-возвраты.** `[[nodiscard]] Lock acquire();` — если результат не сохранить, замок
  тут же освободится (временный объект умрёт), и защита исчезнет молча. `[[nodiscard]]`
  ловит именно это.

### Механика и тонкости

- Это **предупреждение, а не ошибка**. С `-Werror` станет ошибкой; в нашем курсе `-Werror`
  не включён, так что вы просто увидите warning. Это нормально для незавершённого стаба.
- Можно дать причину: `[[nodiscard("проверь код ошибки")]]` (C++20) — текст попадёт в
  диагностику.
- Можно пометить **весь тип**: `struct [[nodiscard]] Error{};` — тогда любая функция,
  возвращающая `Error`, наследует требование.
- **Явный отказ.** Если вы *сознательно* игнорируете результат, скажите это явно:
  `(void)divmod(a, b);` или `std::ignore = ...;` — предупреждение исчезнет, а намерение
  останется видимым в коде.
- Ставьте `[[nodiscard]]` экономно — на функции, где игнор почти всегда ошибка. Если
  навешать на всё подряд, предупреждения станут шумом, и их начнут глушить.

## Идея 8. `std::string_view` — строка без владения

`std::string_view` — это **невладеющее «окно»** в чужую последовательность символов: пара
`{указатель, длина}`. Он ничего не аллоцирует и не копирует — лишь смотрит на уже
существующие байты (внутри `std::string`, строкового литерала, буфера).
```cpp
#include <string_view>
void log(std::string_view msg);   // принимает и std::string, и "литерал", и char* — без копий

std::string s = "hello world";
std::string_view first = std::string_view{s}.substr(0, 5);   // "hello", БЕЗ новой строки
```

### Как это работает под капотом

Внутри `string_view` — всего два поля: `const char* data_` и `size_t size_`. Операции вроде
`substr`, `remove_prefix`, `find` **не трогают байты** — они лишь двигают указатель и/или
меняют длину. Поэтому `substr` у `string_view` — это O(1) и ноль аллокаций, в отличие от
`std::string::substr`, который копирует.

### Зачем это нужно

- **Дешёвые параметры функций.** Принимая `std::string_view` вместо `const std::string&`,
  функция одинаково принимает `std::string`, C-строку и литерал — и ни в одном случае не
  создаёт временную `std::string`.
- **Разбор без копий.** Токенайзеры, парсеры, split: нарезаем большой текст на под-окна,
  не копируя ни байта (этим вы и займётесь в задании 6).

### Trade-offs и САМЫЕ частые ошибки (опасны!)

`string_view` **не владеет** данными — отсюда все его ловушки. Помните: окно живёт ровно
столько, сколько живёт то, на что оно смотрит.

1. **Висячий view на временную строку.** Классика:
   ```cpp
   std::string make();
   std::string_view sv = make();   // make() вернул temporary, он УМЕР -> sv висит, UB
   ```
   Никогда не сохраняйте `string_view` на временный объект дольше выражения.

2. **Возврат view на локальную строку.**
   ```cpp
   std::string_view bad() { std::string local = "x"; return local; }  // local умрёт -> UB
   ```

3. **`string_view` НЕ обязан быть нуль-терминированным.** `substr` даёт окно в середину
   строки — там нет `'\0'` в конце. Передавать `sv.data()` в C-функции вроде `printf("%s")`
   или `fopen` — ошибка: они читают до `'\0'` и уедут за пределы окна.
   ```cpp
   std::string_view part = std::string_view{"abcdef"}.substr(0, 3);  // "abc", но data() -> "abcdef"
   printf("%s\n", part.data());   // НАПЕЧАТАЕТ "abcdef"! data() не учитывает длину
   ```
   Чтобы получить настоящую C-строку, постройте `std::string{part}` (вот тут копия уместна).

4. **Модификация источника инвалидирует view.** `push_back`/`reserve` у `std::string` могут
   переаллоцировать буфер — все смотрящие на него `string_view` становятся висячими.

Правило: **`string_view` — отличный тип для параметра и для короткоживущего разбора;
плохой тип для члена класса и для возврата владеющего результата.**

## Идея 9. Грамотные `move` и `forward`

`std::move` и `std::forward` — два инструмента move-семантики, которые постоянно путают.
Оба на самом деле **не двигают и не пересылают ничего** — это просто приведения типа
(`static_cast`), управляющие тем, какая перегрузка будет выбрана.

### Что такое rvalue и почему это важно

У выражения есть «категория значения». Грубо: **lvalue** — у него есть имя/адрес, оно
переживёт это выражение (`x`, `obj.field`). **rvalue** — временное, готовое умереть
(`x + 1`, `make_string()`). Move-конструктор и move-присваивание выбираются, когда источник
— rvalue: раз объект всё равно умрёт, его «потроха» (буфер, указатель) можно **украсть**, а
не копировать. Это и есть выигрыш move-семантики: O(1) перенос вместо O(n) копии.

### `std::move` — «обращайся с этим как с временным»

`std::move(x)` — это `static_cast<T&&>(x)`. Он **ничего не перемещает**, он лишь говорит:
«считай `x` rvalue, можешь забрать у него ресурсы». Реальное перемещение делает уже
вызванный move-конструктор/оператор.
```cpp
std::string a = "большая строка";
std::string b = std::move(a);   // выбран move-конструктор: b забрал буфер a за O(1)
// a теперь в "valid but unspecified" состоянии — трогать значение нельзя, можно лишь
// присвоить заново или уничтожить
```

**Частые ошибки:**
- **`return std::move(local);`** — антипаттерн. Компилятор и так применит NRVO/перемещение;
  ваш `move` лишь **мешает copy elision** и иногда делает хуже. Возвращайте просто `return local;`.
- **Использование объекта после `move`.** После `std::move(v)` значение `v` не определено —
  читать его нельзя.
- **`std::move` у `const`-объекта** молча даёт копию: из `const T` ресурсы не украсть, и
  выбирается копирующая перегрузка. `move` тут — обман зрения, переноса не будет.

### `std::forward` — «сохрани категорию ровно как пришла» (perfect forwarding)

Проблема возникает в шаблонах. Если параметр объявлен как `T&&` при **выводимом** `T` — это
не rvalue-ссылка, а **forwarding reference** (универсальная ссылка). Её хитрость: внутри
функции параметр, имеющий имя, — **всегда lvalue**, даже если в функцию пришёл rvalue.
Передадите его дальше «как есть» — и rvalue превратится в lvalue, move потеряется.
`std::forward<T>(arg)` восстанавливает исходную категорию по выведенному `T`:
```cpp
template <class T>
auto wrap(T&& x) {                 // T&& здесь — forwarding reference
    return sink(std::forward<T>(x));   // rvalue останется rvalue, lvalue — lvalue
}
```
Правило простое:
- параметр объявлен как `T&&` с **выводимым** `T` → пересылайте через **`std::forward<T>`**;
- у вас «настоящий» именованный объект, который вы отдаёте навсегда → **`std::move`**;
- в остальных случаях не трогайте — пусть копируется.

**Ошибка-близнец:** `std::move` вместо `std::forward` в шаблоне «съест» lvalue-аргумент
(украдёт ресурсы у объекта вызывающего!), а `std::forward` вместо `std::move` у обычного
параметра — лишний шум. Различайте: `forward` — для forwarding reference, `move` — для всего
остального, что отдаёте.

В задании 7 (`ScopeGuard`) вы примените `std::move` грамотно: переместите `std::function` в
поле, а не скопируете.

## Идея 10. `if constexpr` — ветвление внутри шаблона на этапе компиляции

Обычный `if` всегда **компилирует обе ветки** — обе должны быть синтаксически и семантически
корректны независимо от условия. В шаблонном коде это создаёт серьёзную проблему: для разных
типов-параметров одна из веток может быть вовсе не валидна.

```cpp
template <class T>
std::string to_string_impl(T val) {
    if (std::is_integral_v<T>) {
        return std::to_string(val);    // ок для int
    } else {
        return val.to_string();        // НЕ компилируется для int — у него нет метода!
    }
}
```

`if constexpr` (C++17) решает это: **непройденная ветка не инстанцируется**, то есть
компилятор не проверяет её корректность для данного типа.

```cpp
template <class T>
std::string to_string_impl(T val) {
    if constexpr (std::is_integral_v<T>) {
        return std::to_string(val);   // инстанцируется только для целых типов
    } else {
        return val.to_string();       // инстанцируется только для остальных типов
    }
}
```

### Как это работает под капотом

Условие `if constexpr` — обязательно **константное выражение**. Компилятор оценивает его
во время компиляции (при инстанциировании шаблона). Ветка, у которой условие `false`, полностью
**отбрасывается**: её тело не участвует в анализе типов, не генерирует код, не вызывает
поиск имён, не триггерит перегрузку. Другими словами, для этой ветки компилятор делает только
синтаксический разбор (парсинг) — семантику (смысл) он не проверяет.

Вне шаблонов `if constexpr` тоже работает, но смысла не несёт: условие известно и без него, а
обе ветки всё равно должны быть корректны в своём типовом контексте.

### Зачем нужен `if constexpr` вместо SFINAE

До C++17 для похожего эффекта использовался **SFINAE** (`enable_if`, `void_t`): механизм,
при котором «неподходящая» специализация шаблона молча отбрасывалась из множества кандидатов.
SFINAE работает, но синтаксически крайне тяжёл:

```cpp
// SFINAE-подход (до C++17) — трудно читать и отлаживать:
template <class T, std::enable_if_t<std::is_integral_v<T>, int> = 0>
std::string to_str(T val) { return std::to_string(val); }

template <class T, std::enable_if_t<!std::is_integral_v<T>, int> = 0>
std::string to_str(T val) { return val.to_string(); }
```

`if constexpr` заменяет всё это линейным условным блоком — читается как обычный if.

### Частые ошибки и подводные камни

- **`if constexpr` — не макрос и не `#if`.** Отброшенная ветка всё равно **парсится** и
  должна быть синтаксически правильным C++. Просто опечатку она не скроет.
- **Условие обязано быть константным.** `if constexpr (n > 0)` с рантайм-переменной `n` —
  ошибка компиляции.
- **Обе ветки должны корректно возвращать.** Если функция должна что-то вернуть, оба пути
  `if constexpr / else` должны содержать `return` (или один из них всегда недостижим для
  данного типа). Компилятор всё равно проверяет control flow.
- **Не путайте с `if (constexpr ...)` без `else`.** Пропущенный `else` означает, что код после
  блока выполнится в обоих случаях — это обычно не то, что нужно в шаблоне.

```cpp
// Практичный пример: разный вывод для числовых и строковых типов
template <class T>
void pretty_print(const T& val) {
    if constexpr (std::is_arithmetic_v<T>) {
        std::cout << "[num] " << val << "\n";
    } else if constexpr (std::is_same_v<T, std::string>) {
        std::cout << "[str] \"" << val << "\"\n";
    } else {
        std::cout << "[?] " << "\n";  // .to_string() здесь не нужен — ветка просто не вызовется
    }
}
```

## Идея 11. RAII, rule of 0 и rule of 5

**RAII (Resource Acquisition Is Initialization)** — центральная идиома управления ресурсами
в C++. Суть: жизнью ресурса (память, файл, мьютекс, соединение) управляет объект. Ресурс
захватывается в конструкторе и освобождается в деструкторе.

```cpp
// RAII без умных указателей: сами пишем деструктор
struct FileHandle {
    FILE* fp;
    explicit FileHandle(const char* path) : fp(std::fopen(path, "r")) {
        if (!fp) throw std::runtime_error("cannot open file");
    }
    ~FileHandle() { if (fp) std::fclose(fp); }   // ресурс освобождён всегда — даже при исключении
};
```

### Инвариант ресурса

Сильная гарантия RAII: **объект в живом состоянии — ресурс захвачен; деструктор вызван —
ресурс освобождён.** Если конструктор бросил исключение — деструктор не вызывается (объект
не был сконструирован), но деструкторы уже построенных подобъектов вызовутся, что безопасно.

### Rule of 5

Если вы ручным образом управляете ресурсом в классе (пишете деструктор), то компилятор
перестаёт автоматически генерировать корректные copy/move операции. «Правило пяти» гласит:
**если вы объявили хотя бы одно из пяти спецмодифицирующих существований — деструктор,
copy-конструктор, copy-присваивание, move-конструктор, move-присваивание — скорее всего,
нужны все пять** (или явный `= delete` для тех, что не нужны).

```cpp
struct Buffer {
    int* data;
    std::size_t size;

    explicit Buffer(std::size_t n) : data(new int[n]), size(n) {}
    ~Buffer() { delete[] data; }                          // 1. деструктор

    Buffer(const Buffer& o) : data(new int[o.size]), size(o.size) {  // 2. copy-ctor
        std::copy(o.data, o.data + size, data);
    }
    Buffer& operator=(const Buffer& o) {                  // 3. copy-assign
        if (this != &o) { delete[] data; size = o.size; data = new int[size];
                          std::copy(o.data, o.data + size, data); }
        return *this;
    }
    Buffer(Buffer&& o) noexcept : data(o.data), size(o.size) { // 4. move-ctor
        o.data = nullptr; o.size = 0;
    }
    Buffer& operator=(Buffer&& o) noexcept {              // 5. move-assign
        if (this != &o) { delete[] data; data = o.data; size = o.size;
                          o.data = nullptr; o.size = 0; }
        return *this;
    }
};
```

### Rule of 0 — предпочтительный вариант

Rule of 5 многословен и чреват ошибками (например, забыть `if (this != &o)` в copy-assign).
Современный C++ предлагает лучшее решение — **rule of 0**: **не объявляйте ни одного из пяти
спецчленов, если это вообще возможно.** Вместо ручного управления делегируйте владение
умным указателю или стандартному контейнеру, которые уже реализуют rule of 5 правильно.

```cpp
// Rule of 0: никаких спецчленов — компилятор сгенерирует всё корректно
struct Buffer {
    std::vector<int> data;   // vector уже RAII, уже move-aware
    // деструктор, копирование, move — генерируются автоматически и корректно
};
```

Предпочитайте `std::unique_ptr`, `std::shared_ptr`, `std::vector`, `std::string` как
«первичные носители ресурса». Rule of 5 нужен только при написании фундаментальных
низкоуровневых оберток (своего аллокатора, интрузивного контейнера, собственного умного
указателя).

### Связь с move-семантикой

Move-семантика и RAII усиливают друг друга: move позволяет передавать владение ресурсом за
O(1) без копии. Контейнеры и умные указатели стандартной библиотеки уже move-aware. Именно
поэтому `std::unique_ptr` нельзя скопировать, но можно переместить — эта асимметрия
принудительно выражает единственное владение.

### ScopeGuard как иллюстрация RAII

`ScopeGuard` из задания 7 — классический пример ad hoc RAII: лямбда («действие на выходе»)
играет роль «ресурса», который нужно «освободить» при уничтожении объекта. Ресурс захватывается
в конструкторе (`fn_ = std::move(fn)`), «высвобождается» в деструкторе (`fn_()`). Метод
`dismiss()` — аналог «отпустить владение» у `unique_ptr::release()`: ресурс передан наружу или
намеренно отброшен.

## Идея 12. `std::optional` и `std::variant` — типобезопасные «опциональные» значения

### `std::optional` — значение или ничего

`std::optional<T>` (C++17) хранит либо значение типа `T`, либо «ничего» (`std::nullopt`).
Это **типобезопасная альтернатива возврату специальных «сигнальных» значений** (−1, −∞,
пустая строка) или паре `{T value, bool ok}`.

```cpp
#include <optional>

std::optional<int> parse_int(std::string_view s) {
    try {
        return std::stoi(std::string(s));  // возвращаем значение
    } catch (...) {
        return std::nullopt;               // возвращаем «ничего»
    }
}

auto result = parse_int("42");
if (result) {                           // implicit bool
    std::cout << *result << "\n";       // разыменование — только если has_value()
}
// или с умолчанием:
int val = result.value_or(0);
```

**Как это работает под капотом.** `optional<T>` хранит `T` в выровненном буфере внутри себя
(без динамической аллокации) плюс `bool`-флаг наличия. `sizeof(optional<int>) == 8` (или
около того), а не `sizeof(int)` — дополнительный байт на флаг с выравниванием. Операция
`*opt` не проверяет флаг в release-режиме — вы обязаны проверить `has_value()` сами (иначе
UB).

**Зачем.** Явный `optional` в сигнатуре функции говорит читателю: «эта функция может не
вернуть результат, и ты обязан это обработать». Это лучше, чем молчаливый `-1` или исключение
ради обычного «не найдено».

**Ловушки:**
- `*opt` при пустом optional — UB в production, исключение `bad_optional_access` только
  через `opt.value()`. Используйте `has_value()` или `if (opt)` перед `*opt`.
- `optional<T&>` (ссылки) — **запрещён** стандартом. Для опциональной ссылки используйте
  `optional<std::reference_wrapper<T>>` или указатель.
- Не используйте `optional` для «обязательных» параметров. Это семантический запах.

### `std::variant` — type-safe union

`std::variant<T1, T2, ...>` (C++17) хранит ровно одно значение из перечисленного списка
типов — безопасный аналог `union`. В отличие от голого `union`, `variant` отслеживает, какой
тип сейчас активен, и не позволяет обратиться к другому типу без явной проверки.

```cpp
#include <variant>

std::variant<int, double, std::string> v = 42;        // хранит int
v = 3.14;                                              // теперь хранит double
v = std::string("hello");                              // теперь хранит string

// Безопасное извлечение:
if (auto* s = std::get_if<std::string>(&v)) {
    std::cout << "строка: " << *s << "\n";
}
```

**`std::visit` — обход всех вариантов.** Главный паттерн использования `variant` — передать
его в `std::visit` вместе с **вызываемым объектом**, который перегружен для каждого из типов:

```cpp
struct Printer {
    void operator()(int x)               const { std::cout << "int: "    << x << "\n"; }
    void operator()(double x)            const { std::cout << "double: " << x << "\n"; }
    void operator()(const std::string& s)const { std::cout << "string: " << s << "\n"; }
};

std::visit(Printer{}, v);    // диспетчеризация по активному типу — O(1), без if-цепочки
```

Популярный идиом — **overloaded lambdas** через наследование (C++17):
```cpp
template<class... Ts> struct overloaded : Ts... { using Ts::operator()...; };
// ...
std::visit(overloaded{
    [](int x)               { std::cout << "int: "    << x << "\n"; },
    [](double x)            { std::cout << "double: " << x << "\n"; },
    [](const std::string& s){ std::cout << "string: " << s << "\n"; }
}, v);
```

**Как это работает под капотом.** `variant` занимает `max(sizeof(Ts)...)` байт плюс
дискриминатор (индекс активного типа). Объект размещается «inline», без heap. `std::visit`
реализуется через таблицу указателей на функции (vtable-подобная dispatch), выбирая нужную
перегрузку по дискриминатору за O(1).

**Ловушки:**
- `std::get<T>(v)` бросает `std::bad_variant_access`, если активный тип — не `T`. Используйте
  `std::get_if<T>(&v)` (возвращает указатель или `nullptr`) для безопасного доступа.
- `variant` не может быть пустым... почти: при исключении в конструкторе/присваивании он
  может попасть в «valueless_by_exception» состояние. Этот угловой случай редок на практике.
- Не используйте `variant` с одним типом — для этого есть `optional` или просто сам тип.

### `optional` vs исключения vs коды ошибок

| Ситуация | Рекомендация |
|---|---|
| Не найдено / нет значения (обычный случай) | `std::optional<T>` |
| Ошибка программиста (инвариант нарушен) | исключение или assert |
| Высокочастотный путь, overhead исключений неприемлем | `optional` или `expected<T,E>` (C++23) |
| Несколько разных причин отказа | `variant<T, ErrA, ErrB>` или `expected` |

## Идея 13. `std::span` — невладеющий вид на непрерывный диапазон

`std::span<T>` (C++20) — это **пара `{указатель, длина}`**, описывающая непрерывный диапазон
элементов типа `T`. Он ничем не владеет, ничего не копирует и ничего не аллоцирует.

```cpp
#include <span>

void print_all(std::span<const int> data) {     // принимает массив, вектор, std::array — что угодно
    for (int x : data) std::cout << x << " ";
}

std::vector<int> v = {1, 2, 3, 4, 5};
print_all(v);                                   // неявное преобразование vector -> span

int arr[] = {10, 20, 30};
print_all(arr);                                 // и массив тоже — без шаблонов!

// Подстрока без копии:
std::span<const int> middle = std::span(v).subspan(1, 3);  // {2, 3, 4}
```

### Зачем нужен `span`

До C++20 функция, принимающая «кусок массива», была вынуждена выбирать между:
- `const std::vector<int>&` — работает только с вектором;
- `const int* data, size_t n` — C-стиль, небезопасен (легко перепутать порядок и размер);
- шаблонным параметром `template<class R>` — сложно ограничить на «непрерывный диапазон».

`std::span` — **единый невладеющий тип** для всех непрерывных диапазонов: `vector`, `array`,
голый массив, указатель+размер. Функция пишется один раз, работает со всеми.

### Как это работает под капотом

`span` — просто пара `{T* ptr, size_t size}`. `sizeof(std::span<int>) == 2 * sizeof(void*)`.
Все операции — арифметика указателей, без аллокаций. `subspan(offset, count)` возвращает
новый `span`, смотрящий на тот же буфер: `O(1)`.

`span` бывает двух видов:
- **динамический** (`std::span<T>`) — размер хранится в объекте `span`;
- **статический** (`std::span<T, N>`) — размер `N` зафиксирован на этапе компиляции, `sizeof`
  такого `span` — только `sizeof(T*)`, размер из объекта убирается.

### Связь с `string_view`

`std::string_view` — это `std::span<const char>` плюс строковые методы (`find`, `substr` и
т.д.). Та же идиома: невладеющий вид без копий. `span` обобщает эту идею на произвольный тип
элемента.

### Ловушки

- **Висячий span** — та же проблема, что у `string_view`: если источник (вектор, массив)
  уничтожен или переаллоцирован, `span` становится висячим. Срок жизни `span` не должен
  превышать срок жизни источника.
  ```cpp
  std::span<int> bad() {
      std::vector<int> v = {1, 2, 3};
      return v;    // v умрёт -> span висит, UB
  }
  ```
- **`span<T>` не умеет изменять размер.** Нельзя `push_back`. Это вид, не контейнер.
- **Нет нуль-терминации.** В отличие от `std::string`, в `span<char>` нет гарантии `'\0'` в
  конце — передавать `span.data()` в C-функции с той же осторожностью, что и `string_view`.

## Идея 14. Copy elision, RVO и NRVO

Одна из самых неочевидных оптимизаций C++ — **устранение копирования при возврате значения**.
Понимание её механики объясняет, почему `return std::move(local)` — антипаттерн.

### Prvalue materialization и guaranteed copy elision (C++17)

В C++17 введено понятие **prvalue materialization**: prvalue («pure rvalue», например, вызов
функции по значению, литерал, временный объект) — это не объект, а **рецепт создания объекта**.
Материализуется он ровно тогда и там, когда нужен. Отсюда следует **гарантированный copy
elision**: компилятор **обязан** убрать копию при инициализации объекта из prvalue — даже если
copy-конструктор имеет побочные эффекты.

```cpp
std::string make() {
    return std::string("hello");   // prvalue — материализуется прямо в переменную вызывающего
}

std::string s = make();           // ГАРАНТИРОВАННО без копии (C++17) — конструктор вызван один раз
```

Это **не оптимизация**, которую компилятор может пропустить — это требование стандарта.

### RVO (Return Value Optimization)

RVO — частный случай guaranteed copy elision: когда функция возвращает временный объект
(prvalue), построенный внутри `return`-выражения. Гарантирован с C++17.

### NRVO (Named Return Value Optimization)

NRVO работает иначе: когда функция возвращает **именованную локальную переменную** (`return local;`).
В этом случае компилятор **имеет право** (но не **обязан**) построить `local` прямо в
возвращаемом месте вызывающего кода, избегая move/copy при возврате. NRVO — **оптимизация,
а не стандартное требование**.

```cpp
std::vector<int> build(int n) {
    std::vector<int> result;
    for (int i = 0; i < n; ++i) result.push_back(i);
    return result;   // NRVO: компилятор, скорее всего, построит result прямо в вызывающем
}

auto v = build(1000);  // обычно — ноль копий и ноль moves
```

На практике все современные компиляторы (GCC, Clang, MSVC) применяют NRVO в простых случаях.
Но стандарт его не гарантирует.

### Почему `return std::move(local)` — антипаттерн

```cpp
std::vector<int> bad_build(int n) {
    std::vector<int> result;
    // ...
    return std::move(result);   // ПЛОХО!
}
```

Казалось бы, `std::move` должен помочь — передать вектор без копии. Но на самом деле:

1. `std::move(result)` превращает `result` в xvalue (rvalue-ссылку). Это **убивает NRVO**:
   компилятор видит не «верни именованную переменную», а «верни rvalue-expression» — NRVO
   больше не применим. Функция теперь обязана сначала построить `result`, потом выполнить
   move в возвращаемое место.
2. Без `std::move` при невозможности NRVO компилятор **всё равно применяет неявный move**
   (стандарт гарантирует это): он пробует move-конструктор перед copy-конструктором для
   именованной локальной переменной в `return`.

Итог: `return std::move(local)` — в лучшем случае та же скорость, что `return local;`, в
худшем — медленнее (нет NRVO). Пишите просто `return local;`.

**Исключение:** если вы возвращаете **параметр функции** (не локальную переменную) или
объект, у которого move явно предпочтителен и NRVO не применим, явный `std::move` обоснован.
Но для локальных переменных — никогда.

### Практические выводы

| Случай | Что делать |
|---|---|
| Возврат временного объекта | `return T{...};` — гарантированный elision (C++17) |
| Возврат именованной локальной переменной | `return local;` — NRVO применится автоматически |
| Хотите «помочь» компилятору | Не трогайте. `return std::move(local)` мешает, не помогает |
| Возврат параметра функции | `return std::move(param);` — здесь NRVO не применим, move уместен |

---

## Задания

Файл `src/task10.cpp` (интерфейс — `include/task10.hpp`). `square_ce` уже определена в
заголовке как `constexpr` — разберись, почему она там, а не в `.cpp` (спроси, если надо).

1. `std::pair<int,int> minmax_of(const std::vector<int>& v)` — пара `{минимум, максимум}`
   (вектор непустой). Внутри попробуй structured bindings.
2. `enum class Color { Red, Green, Blue }` (объявлен в заголовке) и
   `std::string color_name(Color c)` — `"Red"`, `"Green"`, `"Blue"`.
3. `constexpr int square_ce(int n)` — квадрат, **на этапе компиляции** (определена в
   заголовке; тест проверит `static_assert`). Реализуй её там.
4. `std::vector<int> evens_squared(const std::vector<int>& v)` — оставить чётные и
   возвести их в квадрат. Сделай через `std::views::filter | std::views::transform`.
5. `int sum_values(const std::map<std::string,int>& m)` — сумма всех значений; обходи
   через `for (const auto& [k, val] : m)`.

Дальше — три задания посложнее (новые функции/класс уже объявлены в `include/task10.hpp`;
реализуй `split_view` и `divmod` в `src/task10.cpp`, а `ScopeGuard` — прямо в заголовке).

6. `std::vector<std::string_view> split_view(std::string_view s, char sep)` — разбей `s`
   по символу `sep` **без единой копии строки**. Каждый кусок — `string_view`, смотрящий
   внутрь исходной `s`. Пустые куски сохраняем: `","` → `{"", ""}`, `""` → `{""}`,
   `"a,b,c"` → `{"a","b","c"}`. Тест проверит, что `parts[i].data()` указывает **внутрь**
   исходной строки (то есть копии действительно нет).
7. `class ScopeGuard` (объявлен в заголовке) — выполняет переданную лямбду **в деструкторе**
   («finally»): что бы ни случилось — обычный выход, `return`, исключение — действие
   сработает ровно один раз. Метод `dismiss()` отменяет действие. Класс некопируемый;
   `std::function` в поле клади через `std::move`. Реализуй тело `dismiss()` и `~ScopeGuard()`.
8. `[[nodiscard]] std::pair<int,int> divmod(int a, int b)` — вернуть `{a / b, a % b}`.
   При `b == 0` бросить `std::invalid_argument`. Результат разбирается у вызывающего через
   structured bindings: `auto [q, r] = divmod(...)`. Атрибут `[[nodiscard]]` означает, что
   проигнорировать результат компилятор не даст молча.

```bash
./cpp/run.sh 10
```

### Подсказки

<details><summary>Задание 3 — почему constexpr в заголовке</summary>
Чтобы вычислить функцию на этапе компиляции в другом файле, компилятор должен видеть
её ТЕЛО. Как и шаблоны (модуль 07), `constexpr`-функции обычно живут в заголовках.
</details>
<details><summary>Задание 4 — материализация view</summary>
`std::views::filter(...) | std::views::transform(...)` даёт ленивое представление.
Чтобы вернуть `std::vector<int>`, пройди по нему и собери элементы (например, циклом
`for (int x : view) out.push_back(x);`).
</details>

<details><summary>Задание 6 — split_view, нудж</summary>
Тебе нужны только позиции разделителей; сами байты двигать не надо. У `string_view` есть
`find(ch, from)`, возвращающий индекс или `std::string_view::npos`, и `substr(start, len)`,
который возвращает <b>новый string_view</b> в ту же память (копии нет).
</details>
<details><summary>Задание 6 — структура цикла</summary>
Веди указатель <code>start = 0</code>. На каждом шаге ищи следующий разделитель
<code>p = s.find(sep, start)</code>. Если нашёлся — кусок это <code>s.substr(start, p - start)</code>,
после чего <code>start = p + 1</code>. Если не нашёлся (<code>npos</code>) — последний кусок это
<code>s.substr(start)</code> (до конца), и выходи из цикла. Подумай: почему такой цикл сам
собой даёт пустые куски для <code>","</code> и для <code>""</code>, и почему он всегда кладёт
хотя бы один элемент?
</details>
<details><summary>Задание 7 — ScopeGuard, нудж</summary>
Вся идея — в деструкторе. У тебя есть поле <code>fn_</code> (лямбда) и флаг <code>active_</code>.
<code>dismiss()</code> должен лишь снять флаг. Деструктор: если действие ещё активно и лямбда
непустая — вызвать её.
</details>
<details><summary>Задание 7 — детали деструктора</summary>
Проверяй и флаг, и саму функцию: <code>if (active_ && fn_) fn_();</code>. Деструктор не должен
выпускать исключение наружу — если боишься, что лямбда бросит, оберни вызов в
<code>try { fn_(); } catch (...) {}</code>. Помни: тест с копированием запрещён не зря —
копия выполнила бы действие дважды, поэтому копирование уже <code>= delete</code> в заголовке.
</details>
<details><summary>Задание 8 — divmod</summary>
Сначала граница: <code>if (b == 0) throw std::invalid_argument("...");</code> (нужен
<code>#include &lt;stdexcept&gt;</code>). Иначе верни <code>{a / b, a % b}</code> — встроенные
<code>/</code> и <code>%</code> для <code>int</code> уже дают частное и остаток с усечением к нулю
(поэтому <code>-7 / 2 == -3</code>, <code>-7 % 2 == -1</code>). Атрибут <code>[[nodiscard]]</code>
менять не надо — он уже стоит в объявлении.
</details>

9. `std::optional<int> safe_divide(int a, int b)` — вернуть `a / b` как `optional<int>`,
   или `std::nullopt`, если `b == 0`. **Никаких исключений.** Это альтернативный стиль
   обработки ошибок по сравнению с `divmod` (задание 8): там ошибка — исключение, здесь —
   «пустое значение». Выбор зависит от того, является ли деление на ноль «нормальным»
   сценарием или программной ошибкой.

<details><summary>Задание 9 — safe_divide, нудж</summary>
Тело функции умещается в три строки. Проверь делитель: если <code>b == 0</code> —
верни <code>std::nullopt</code>. Иначе верни <code>a / b</code> — <code>optional&lt;int&gt;</code>
конструируется из значения неявно, или явно: <code>return std::optional&lt;int&gt;{a / b};</code>.
Не забудь <code>#include &lt;optional&gt;</code> в заголовке.
</details>

Сравни свой `evens_squared` с тем, как ту же задачу решали в модуле 08 алгоритмами.
Какой вариант читается лучше? Это хороший повод для разговора о вкусе в коде.

Сравни `safe_divide` (задание 9) и `divmod` (задание 8): оба решают деление, но с разной
стратегией ошибок. Обсуди: когда деление на ноль — исключительная ситуация (исключение),
а когда — ожидаемый «нет результата» (optional)?
